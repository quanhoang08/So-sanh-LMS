global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  # cAdvisor exposes container CPU/memory/network metrics without changing app code.
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]

