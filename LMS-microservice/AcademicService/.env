# SERVER
PORT=3000
NODE_ENV=development

# DATABASE
DB_HOST=academic-db          # ← SỬA TỪ "localhost" thành "postgres-db"
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=123456
DB_NAME=lms_academic         # giữ nguyên

# JWT (Phải giống hệt service kia)
JWT_SECRET=super_secret_key_for_lms_2024
JWT_EXPIRES_IN=1d

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
