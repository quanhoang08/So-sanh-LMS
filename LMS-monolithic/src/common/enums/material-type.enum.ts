// DB: CREATE TYPE material_type AS ENUM ('image', 'video', 'audio', 'document');
export enum MaterialType {
  IMAGE    = 'image',
  VIDEO    = 'video',
  AUDIO    = 'audio',
  DOCUMENT = 'document',
}